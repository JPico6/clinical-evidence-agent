# Secrets
.env
.env.*
!.env.example
.streamlit/secrets.toml

# Python / tooling
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
.tmp/

# Editors / OS
.idea/
.vscode/
.DS_Store

# Local full Synthea export. The small curated public demo is safe to commit.
data/synthea/
!data/public_demo/
!data/public_demo/*.csv

# Raw paid-evaluation payloads are retained locally but not needed in the
# public portfolio repository. Keep the compact review artifacts instead.
evaluation_runs/multipatient_evaluation.json
evaluation_runs/multipatient_smoke.json
